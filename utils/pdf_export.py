def generer_pdf_rapport(profil, recommandations, economies_totales):
    """Fonction placeholder - PDF désactivé pour le moment"""
    import io
    buffer = io.BytesIO()
    buffer.write(b'PDF temporairement desactive')
    buffer.seek(0)
    return buffer
